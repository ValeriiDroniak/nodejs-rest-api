extends layout/base

block content
  table(align="center" role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%" style="margin: auto;")
    tbody
      tr
        td(valign="top" class="bg-white" style="padding: 1em 2.5em 0 2.5em;")
          table(role="presentation" border="0" cellpadding="0" cellspacing="0" width="100%")
            tbody
              tr
                td.logo(style="text-align: center;")
                  h1
                    a(href="#") V&S APP
      tr
        td(valign="middle" class="hero bg-white" style="padding: 2em 0 4em 0;")
          table
            tbody
              tr
                td
                  .text(style="padding: 0 2.5em; text-align: center;")
                    h2 Hello! #{name}
                    h3 To successfully verify your email, just click on the button
                    p
                      a.btn.btn-primary(href=`${url}`, target="_blank")
                        | Confirm